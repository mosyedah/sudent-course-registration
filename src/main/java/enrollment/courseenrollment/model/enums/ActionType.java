package enrollment.courseenrollment.model.enums;

public enum ActionType {
    SIGN_UP, LOGIN, VIEW_PROFILE, UPDATE_PROFILE,
    ENROLL, DROP, WAITLISTED, PASSWORD_UPDATE, OPTED_OUT, ENROLLED_FROM_WAITLIST
}
